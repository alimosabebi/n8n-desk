/**
 * Markdown rendering stub.
 * Will be replaced with a proper markdown-to-HTML parser
 * when the chat UI is implemented.
 */
export function renderMarkdown(input: string): string {
  return input
}
