/**
 * HTML sanitization stub.
 * Will be replaced with a proper sanitizer (DOMPurify or similar)
 * when the chat UI is implemented.
 */
export function sanitizeHtml(input: string): string {
  return input
}
